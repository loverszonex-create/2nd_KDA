# EfficientNet-B0 이미지 분류 튜토리얼 의존성
# 설치 명령어: pip install -r requirements.txt

# 딥러닝 프레임워크
torch>=2.0.0
torchvision>=0.15.0

# 이미지 처리
Pillow>=9.0.0
opencv-python>=4.7.0

# 수치 계산 및 데이터 처리
numpy>=1.21.0
matplotlib>=3.5.0

# HTTP 요청 (URL 이미지 다운로드용)
requests>=2.28.0

# 선택적 의존성 (성능 향상)
# CUDA 지원이 필요한 경우 아래 주석 해제
# torch-audio>=2.0.0

# Jupyter 노트북에서 사용하는 경우
# jupyter>=1.0.0
# ipywidgets>=8.0.0

# 추가 이미지 포맷 지원
# imageio>=2.25.0
# scikit-image>=0.19.0
